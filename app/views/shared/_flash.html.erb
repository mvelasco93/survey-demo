<div class="container">
  <div class="flash">
    <% flash.each do |name, msg| %>
      <p class="alert alert-<%= correct_flash_name(name) %>">
        <%= msg %>
      </p>
    <% end %>
  </div>
</div>